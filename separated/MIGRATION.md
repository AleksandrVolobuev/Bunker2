# 🔄 Миграция на раздельную архитектуру

## 📦 Что изменилось?

### Было (старая структура):
```
~/Projects/Bunker/server/
├── server.js          # Сервер + раздача статики
├── public/
│   └── index.html     # Клиент
└── package.json
```

### Стало (новая структура):
```
bunker-game/
├── client/            # 🎨 Отдельный клиент
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
│
└── server/            # ⚙️ Отдельный сервер
    ├── server.js
    └── package.json
```

---

## 🚀 Как перейти на новую структуру?

### Вариант 1: Продолжить как есть (ничего не делать)

Ваш текущий проект в `~/Projects/Bunker/server/` **продолжит работать!**

Просто:
```bash
cd ~/Projects/Bunker/server
npm start
```

И всё работает как раньше! ✅

---

### Вариант 2: Перейти на раздельную архитектуру

#### Шаг 1: Создайте новую структуру

```bash
# Создайте новую папку для проекта
mkdir -p ~/Projects/bunker-separated
cd ~/Projects/bunker-separated

# Скопируйте файлы из outputs/separated/
# (загрузите их с сайта)
```

#### Шаг 2: Запустите сервер

```bash
cd ~/Projects/bunker-separated/server
npm install
npm start
```

Сервер запустится на http://localhost:3000

#### Шаг 3: Запустите клиент (в новом терминале)

```bash
cd ~/Projects/bunker-separated/client
npm install
npm run dev
```

Клиент запустится на http://localhost:5173

#### Шаг 4: Откройте игру

Откройте http://localhost:5173 в браузере!

---

## 🌐 Ngrok с новой структурой

### Вариант A: Туннель только на сервер (рекомендуется)

```bash
# Терминал 1: Сервер
cd ~/Projects/bunker-separated/server
npm start

# Терминал 2: Клиент (локально)
cd ~/Projects/bunker-separated/client
npm run dev

# Терминал 3: Ngrok на сервер
~/ngrok http 3000
```

Получите ссылку: `https://xxx.ngrok-free.app`

В `client/index.html` измените:
```javascript
const SERVER_URL = 'https://xxx.ngrok-free.app';
```

Друзья подключаются через http://localhost:5173 (если у вас дома) или деплойте клиент.

### Вариант B: Туннель на клиент (проще для друзей)

```bash
# Терминал 1: Сервер
cd ~/Projects/bunker-separated/server
npm start

# Терминал 2: Клиент
cd ~/Projects/bunker-separated/client
npm run dev

# Терминал 3: Ngrok на клиент
~/ngrok http 5173
```

Друзья подключаются по ссылке ngrok!

---

## 💡 Что лучше выбрать?

### Оставить как есть, если:
- ✅ Вам удобно текущее решение
- ✅ Играете только локально или через ngrok
- ✅ Не планируете менять дизайн часто

### Перейти на раздельную, если:
- ✅ Хотите деплоить клиент и сервер отдельно
- ✅ Планируете добавлять фичи
- ✅ Хотите использовать React/Vue в будущем
- ✅ Нужна быстрая разработка с HMR

---

## 🎯 Рекомендация

**Для обучения и экспериментов:**  
Оставьте старую структуру - она работает!

**Для серьёзного проекта:**  
Используйте раздельную архитектуру.

---

## 🔧 Сравнение

| Характеристика | Старая структура | Новая структура |
|---------------|------------------|-----------------|
| Простота | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Гибкость | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| Скорость разработки | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Деплой | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| Масштабируемость | ⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## ❓ Вопросы и ответы

**Q: Нужно ли мигрировать?**  
A: Нет, старый проект продолжит работать.

**Q: Можно ли использовать обе структуры?**  
A: Да! Они независимы.

**Q: Как ngrok работает с новой структурой?**  
A: Точно так же! Просто направьте на порт сервера (3000) или клиента (5173).

**Q: Потеряю ли я данные?**  
A: Нет, это просто другая организация файлов.

---

## 📝 Итог

У вас теперь **два варианта**:

1. **Старый проект** в `~/Projects/Bunker/server/` - работает как раньше
2. **Новый проект** в `separated/` - более продвинутая архитектура

Выбирайте то, что вам удобнее! 🎉