# 🎮 Бункер - Клиентская часть

Фронтенд для игры "Бункер".

## 🚀 Быстрый старт

### Разработка (локально)

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

Откройте http://localhost:5173

### Продакшен сборка

```bash
# Сборка для продакшена
npm run build

# Просмотр продакшен версии
npm run preview
```

## ⚙️ Конфигурация

### Подключение к серверу

По умолчанию клиент подключается к серверу по адресу текущей страницы.

Чтобы подключиться к другому серверу, измените в `index.html`:

```javascript
const SERVER_URL = 'https://your-server.ngrok-free.app';
```

### Прокси для разработки

В `vite.config.js` настроен прокси на `http://localhost:3000`.

Если ваш сервер на другом порту, измените:

```javascript
proxy: {
  '/socket.io': {
    target: 'http://localhost:ВАШИ_ПОРТ',
    ws: true,
    changeOrigin: true
  }
}
```

## 📦 Деплой

### Деплой на Vercel

```bash
npm install -g vercel
vercel
```

### Деплой на Netlify

```bash
npm install -g netlify-cli
netlify deploy --prod --dir=dist
```

### Деплой на GitHub Pages

1. Соберите проект: `npm run build`
2. Загрузите содержимое папки `dist/` в GitHub Pages

## 🔧 Разработка

### Структура проекта

```
client/
├── index.html          # Главная страница
├── package.json        # Зависимости
├── vite.config.js      # Конфигурация Vite
└── dist/              # Продакшен сборка (после npm run build)
```

### Технологии

- **Vite** - быстрая сборка
- **Socket.IO Client** - WebSocket подключение
- **Vanilla JS** - без фреймворков

## 🌐 Подключение к удалённому серверу

Если сервер развёрнут отдельно (например на Heroku, Railway, etc):

1. Узнайте адрес сервера (например: `https://bunker-server.railway.app`)
2. Измените в `index.html`:
   ```javascript
   const SERVER_URL = 'https://bunker-server.railway.app';
   ```
3. Пересоберите: `npm run build`
4. Задеплойте клиент

## 📝 Примечания

- Клиент работает независимо от сервера
- Можно деплоить на разные хостинги
- Vite обеспечивает быструю разработку с HMR